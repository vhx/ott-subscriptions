import React from 'react';
import Pagination from './pagination';

// Feedback: Consider removing this as a component and simply placing the pagination component in the main body.

const Footer = () => (
  <div className="footer">
    <Pagination/>
  </div>
)


export default Footer;
